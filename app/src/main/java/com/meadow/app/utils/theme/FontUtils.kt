package com.meadow.app.utils.theme

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.meadow.R

/**
 * FontUtils.kt
 *
 * Loads your kawaii bubble fonts and manages scaling for accessibility.
 */
object FontUtils {
    val Bubble = FontFamily(Font(R.font.bubble_round))
    val Handwritten = FontFamily(Font(R.font.cute_handwritten))
    val Script = FontFamily(Font(R.font.sparkly_script))
}
