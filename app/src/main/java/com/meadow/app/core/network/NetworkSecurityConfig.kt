package com.meadow.app.core.network

/**
 * NetworkSecurityConfig.kt
 *
 * Placeholder configuration for secure networking and SSL pinning.
 * You can extend this later if you handle custom API calls or self-hosted servers.
 */

object NetworkSecurityConfig {
    // For Firebase and Google APIs, this can usually remain empty
    // But if you add your own backend server, define certificate pins here.
}
